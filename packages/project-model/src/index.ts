/**
 * @code-analyzer/project-model — Phase 1 (Repository Intelligence).
 *
 * Not implemented in Phase 0. This package will implement `RepositoryDiscoverer` from
 * `@code-analyzer/core` (see packages/core/src/analyzer/pipeline.ts): filesystem discovery,
 * ignore handling, source classification, generated-code detection, language/framework/package-
 * manager detection, producing a normalized `ProjectModel`.
 *
 * See docs/tasks/phase-1-repository-discovery.md for the task spec once Phase 0 is accepted.
 */
export {};
