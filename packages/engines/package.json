{
  "name": "@code-analyzer/engines",
  "version": "0.0.0",
  "description": "Engine-level composition of analyzers into subsystems (SecurityEngine, ArchitectureEngine, PerformanceEngine) plus the Finding Correlation Engine and Risk Engine.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@code-analyzer/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.14.0"
  }
}
