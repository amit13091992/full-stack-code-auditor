{
  "name": "@code-analyzer/parser",
  "version": "0.0.0",
  "description": "Phase 2: AST/semantic source model (TypeScript Compiler API for JS/TS, per ADR-0006; Tree-sitter reserved for future non-JS/TS languages), symbol resolution, incremental parsing.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@code-analyzer/core": "workspace:*",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0"
  }
}
